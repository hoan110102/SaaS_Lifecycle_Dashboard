# dbt_project.yml
name: 'customer_360_lifecycle'
version: '1.0.0'
config-version: 2

profile: 'customer_360_lifecycle'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  customer_360_lifecycle:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts
      core:
        +materialized: table
        +schema: marts
      analytics:
        +materialized: table
        +schema: marts